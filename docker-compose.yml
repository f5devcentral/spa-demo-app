version: "3.3"
services:
  mongodb:
    image: mongo
    container_name: mongodb
    ports:
      - 27017:27017
    volumes:
      - ./mongo-seed:/docker-entrypoint-initdb.d
    restart: unless-stopped
    environment:
      - SEED_FILE=coffeeProducts.json

  api:
    build: ./api
    container_name: api
    ports:
      - "8000:8000"
    links:
      - mongodb
    environment:
      - MONGO_URL=mongodb
      - INVENTORY_URL=http://inventory:8002
      - RECOMMENDATIONS_URL=http://recommendations:8001
      - IMAGE_DIRECTORY=coffee
    dns:
      - 10.1.1.6
    restart: unless-stopped

  spa:
    build:
      context: ./spa
      args:
        VUE_APP_GLOBAL_COLOR: "#000"
        VUE_APP_GLOBAL_BACKGROUND_COLOR: "#FFF"
    container_name: spa
    ports:
      - "8081:80"
    links:
      - api
    restart: unless-stopped

  recommendations:
    build: ./recommendations
    container_name: recs
    ports:
      - "8001:8001"
    links:
      - api
    restart: unless-stopped

  inventory:
    build: ./inventory
    container_name: inv
    ports:
      - "8002:8002"
    links:
      - api
    restart: unless-stopped
